# Agent Zero — SKI Orchestrator
# Basiert auf dem offiziellen frdel/agent-zero Image
FROM frdel/agent-zero:latest

# Das Image bringt alles mit (Python, Dependencies, Entrypoint).
# Konfiguration erfolgt über Environment-Variablen und Volume-Mounts.
